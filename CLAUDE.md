# Claude Code notes

Follow [`AGENTS.md`](AGENTS.md) as the repository-wide contribution and safety
guide. The frontend-specific architecture, commands, API boundary, deployment,
and security guidance are in `docs/ARCHITECTURE.md`, `docs/DEVELOPMENT.md`,
`docs/API-INTEGRATION.md`, `docs/DEPLOYMENT.md`, and `docs/SECURITY.md`.

This is a standalone Git repository. Workspace sibling files are not guaranteed
to exist in a clone; prefer links and paths inside this repository. Do not stage,
commit, push, or deploy without explicit user authorization.
